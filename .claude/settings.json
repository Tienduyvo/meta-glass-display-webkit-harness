{
  "_comment": "Shipped with the harness. The PostToolUse hook auto-runs the verify gate (tools/check.py + tools/flowtest.py via tools/verify_hook.py) whenever an app config, the registry, or a launcher/worker file changes — so a broken app is caught the instant it's edited and fixed in the same turn. Keep `wrangler dev` running for the flowtest half.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          { "type": "command", "command": "python tools/verify_hook.py" }
        ]
      }
    ]
  }
}
