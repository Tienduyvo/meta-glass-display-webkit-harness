{
  "_comment": "Apps shown in the launcher. Add an entry and its config here (or use tools/new_app.py) — it appears automatically, no new setup. 'config' paths are relative to app/index.html (the launcher).",
  "apps": [
    { "name": "Buy List", "icon": "🛒", "config": "../apps/buy-list/app.config.json" },
    { "name": "To-Do",    "icon": "✅", "config": "../apps/todo/app.config.json" }
  ]
}
